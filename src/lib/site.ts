/**
 * Update this to the real production domain once the site is deployed.
 * Used by metadataBase, sitemap.ts, and robots.ts so it only needs
 * changing in one place.
 */
export const SITE_URL = "https://www.regainr.com";
